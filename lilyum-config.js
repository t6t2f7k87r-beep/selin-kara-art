/*
 * Public browser configuration only. Never place a service-role key here.
 * Until Supabase is connected, the development branch uses isolated demo accounts.
 */
window.LILYUM_CONFIG = Object.freeze({
  supabaseUrl: '',
  supabasePublishableKey: '',
  siteUrl: 'https://lilyumdesigns.com',
  demoMode: true
});
